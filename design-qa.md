final result: passed

Reference target: Product Design directions 1 + 3 for ARBOR Command OS.

Checked:
- Dashboard uses a full command-center shell instead of the previous card-heavy dashboard.
- First viewport now starts with decision rows, KPI strip, and dispatch board, matching the priority structure from reference 1.
- Dispatch board keeps team/time-grid structure with a right-side decision queue, matching reference 3.
- Sidebar is restyled into a dark operational navigation rail.
- Dashboard and Harmonogram now share the new command navigation rail.
- Harmonogram route verified with dispatch panel and calendar surface present after data load.
- Zlecenia, CRM hub, CRM Dashboard, and CRM Pipeline now use the command navigation rail.
- Zlecenia and CRM Pipeline verified at desktop width with no legacy sidebar present.
- Klienci, Ekipy, Flota, and Raporty/RaportyCentrum now use the command navigation rail.
- Klienci, Ekipy, Flota, and Raporty verified in browser with no legacy sidebar present.
- Desktop layout verified at 1600x1000. Build passes with `npm run build -w arbor-web`.

Remaining P3 polish:
- Existing sidebar content is still generated by the shared Sidebar component, so labels and grouping are not a pixel-perfect match to the generated mockup.
- Some legacy dashboard strings still come from existing app translations/data and can be cleaned in a later copy pass.
