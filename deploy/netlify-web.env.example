# Netlify -> Site configuration -> Environment variables
# The web app expects the OS API URL. You may include /api or provide only
# the HTTPS host; web/src/utils/apiBase.js appends /api for HTTPS hosts.

REACT_APP_API_URL=https://<arbor-os>.onrender.com/api
